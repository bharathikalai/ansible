

install the apache2 on ubuntu server

ansible all -i hosts -m apt -a "name=apache2 state=absent" --become


#install == present

#uninstall == absent

#start == started

#stop == stopped

#restart == restarted


# simple command

ansible all -i hosts -a "uname -a"

ansible all -i hosts -a "ping"